window.SDS_CONFIG = {
  // Empty means same-origin, which works when the Node API serves the frontend.
  // For GitHub Pages, set this to the deployed API origin, e.g. https://api.example.com.
  apiBaseUrl: '',
};
